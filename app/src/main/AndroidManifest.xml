<?xml version="1.0" encoding="utf-8"?>
<!-- TODO 01. **DONE** Change the package name of the app. New package name: com.ualr.firstapp" -->
<!-- TODO 02. **DONE** Change the app name. New app name: First App -->
<!-- TODO 03. **DONE** Change the app icon. ic_country image with light blue background.-->

<!-- Comments are Not Allowed inside Manifest Tag!!!! -->
<!-- android:exported="true" required for api 30 and above -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.ualr.firstapp.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>